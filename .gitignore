__pycache__/
*.pyc

# Real local secrets — tracked templates are the .example versions instead.
.env
config/odoo.conf
Caddyfile

# DB dumps are transferred to the server directly, not versioned
backups/
